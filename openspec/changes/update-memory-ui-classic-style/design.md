# Design: 记忆管理分层与经典 macOS 界面风格

## Context

当前 workflow 面板使用现代紫色渐变风格，记忆管理只展示长期记忆。用户希望：
1. 将工作记忆和长期记忆分开管理
2. 采用经典 macOS 风格的界面设计

## Goals / Non-Goals

### Goals
- 在 UI 层面区分工作记忆和长期记忆
- 实现经典 Mac OS 9/Classic 风格的视觉设计
- 保持所有现有功能的可用性

### Non-Goals
- 不改变后端数据结构
- 不引入新的依赖库
- 不改变核心业务逻辑

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    Workflow Panel (Classic Style)                │
├─────────────────────────────────────────────────────────────────┤
│  [聊天历史] [工作记忆] [长期记忆]                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  工作记忆面板 (Tab 2)                                     │    │
│  │  ┌─────────────────────────────────────────────────┐    │    │
│  │  │  Session: abc-123-def                            │    │    │
│  │  │  Topic: Python 编程讨论                           │    │    │
│  │  │  Turns: 5                                        │    │    │
│  │  │  Last Emotion: curious                           │    │    │
│  │  │  Context Variables: {...}                        │    │    │
│  │  └─────────────────────────────────────────────────┘    │    │
│  │                                              [清除记忆]  │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  长期记忆面板 (Tab 3)                                     │    │
│  │  ┌────────────────────────────────────────────────────┐ │    │
│  │  │  [全部] [偏好] [事实] [模式]  搜索: [_______] [🔍] │ │    │
│  │  ├────────────────────────────────────────────────────┤ │    │
│  │  │ ☐ │ 类别    │ 键名          │ 值      │ 操作    │ │    │
│  │  ├────────────────────────────────────────────────────┤ │    │
│  │  │ ☐ │ 偏好    │ language      │ 简洁风格 │ [编辑]  │ │    │
│  │  │ ☐ │ 事实    │ programming   │ Python  │ [编辑]  │ │    │
│  │  │ ☐ │ 模式    │ time_pattern  │ 晚间活跃 │ [编辑]  │ │    │
│  │  └────────────────────────────────────────────────────┘ │    │
│  │  [+ 添加] [批量删除] [全部清除] [导出]                    │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Decisions

### Decision 1: 经典 macOS 设计系统

采用 Mac OS 9 / Classic 风格的视觉设计语言：

**Color Palette**:
```css
:root {
  /* 经典 Mac 系统颜色 */
  --classic-bg: #dddddd;           /* 经典灰色背景 */
  --classic-bg-light: #eeeeee;     /* 浅灰色 */
  --classic-bg-dark: #cccccc;      /* 深灰色 */
  --classic-border-light: #ffffff; /* 高光边框 */
  --classic-border-dark: #888888;  /* 阴影边框 */
  --classic-border-darker: #555555;/* 深色边框 */

  /* 窗口颜色 */
  --classic-window-title: #e0e0e0; /* 窗口标题栏 */
  --classic-window-content: #ffffff; /* 窗口内容区 */

  /* 选中颜色 */
  --classic-selection: #3366cc;    /* 经典蓝色选中 */
  --classic-selection-text: #ffffff;

  /* 文本颜色 */
  --classic-text: #000000;
  --classic-text-secondary: #666666;
  --classic-text-disabled: #999999;
}
```

**Typography**:
```css
/* 经典系统字体 */
font-family:
  "Geneva",
  "Chicago",
  -apple-system,
  BlinkMacSystemFont,
  "Helvetica Neue",
  sans-serif;

/* 固定宽度字体 */
font-family-mono:
  "Monaco",
  "Courier New",
  monospace;
```

**3D Border Effects (Bevel)**:
```css
/* 凸起效果 (Raised) */
.raised {
  border: 2px solid;
  border-color:
    var(--classic-border-light)
    var(--classic-border-dark)
    var(--classic-border-dark)
    var(--classic-border-light);
}

/* 凹陷效果 (Inset) */
.inset {
  border: 2px solid;
  border-color:
    var(--classic-border-dark)
    var(--classic-border-light)
    var(--classic-border-light)
    var(--classic-border-dark);
}

/* 刻槽效果 (Groove) */
.groove {
  border: 2px groove var(--classic-bg);
}
```

**Rationale**: Mac OS 9 的设计语言清晰、功能导向，与现代扁平化设计形成对比，提供独特的视觉体验。

### Decision 2: Tab 结构重组

将现有的 2 个 Tab 扩展为 3 个：

| Tab | 名称 | 内容 |
|-----|------|------|
| 1 | 聊天历史 | 保持现有功能不变 |
| 2 | 工作记忆 | 新增：展示当前会话的工作记忆 |
| 3 | 长期记忆 | 重命名：原"记忆管理"Tab |

**工作记忆面板内容**:
- Session ID
- Current Topic
- Turn Count
- Last Emotion
- Context Variables (JSON 展示)
- 清除按钮
- **添加到长期记忆按钮**

**Rationale**: 分离展示让用户明确理解两种记忆的区别和用途。

### Decision 6: 工作记忆转存到长期记忆

工作记忆面板支持将当前会话信息保存为长期记忆，实现记忆的持久化。

**交互流程**:
```
┌─────────────────────────────────────────────────────────────────┐
│  工作记忆面板                                                     │
├─────────────────────────────────────────────────────────────────┤
│  Session: abc-123-def                                           │
│  Topic: Python 编程讨论                          [+ 保存到长期记忆] │
│  Turns: 5                                                       │
│  Last Emotion: curious                                          │
│  Context Variables:                                             │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  {                                                       │    │
│  │    "discussed_topics": ["Python", "异步编程"],             │    │
│  │    "user_skill_level": "intermediate"                    │    │
│  │  }                                                       │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  [清除记忆]                                                       │
└─────────────────────────────────────────────────────────────────┘
          │
          │ 点击 [+ 保存到长期记忆]
          ▼
┌─────────────────────────────────────────────────────────────────┐
│  添加到长期记忆                                          [×]     │
├─────────────────────────────────────────────────────────────────┤
│  类别: [偏好 ▼]                                                  │
│                                                                  │
│  键名: [discussion_topic_________________]                       │
│                                                                  │
│  值:   [Python 编程讨论____________________]                      │
│        (预填充自 current_topic)                                  │
│                                                                  │
│  置信度: [0.8____] (0.0 - 1.0)                                   │
│                                                                  │
│                                    [取消]  [保存到长期记忆]        │
└─────────────────────────────────────────────────────────────────┘
```

**转存场景**:

| 来源字段 | 默认类别 | 建议键名 | 说明 |
|----------|----------|----------|------|
| current_topic | fact | topic_interest | 用户感兴趣的话题 |
| context_variables 中的偏好 | preference | 自定义 | 用户偏好设置 |
| context_variables 中的事实 | fact | 自定义 | 关于用户的事实 |
| last_emotion 模式 | pattern | emotion_pattern | 情绪模式 |

**API 调用**:
```python
# 复用现有的长期记忆创建 API
POST /memory/long-term
{
  "category": "fact",
  "key": "discussion_topic",
  "value": "Python 编程讨论",
  "confidence": 0.8,
  "source": "user_stated"  # 用户主动保存，标记为 user_stated
}
```

**Rationale**:
- 允许用户主动将有价值的会话信息持久化
- 复用现有的长期记忆创建 API，无需新增后端接口
- 预填充字段降低用户输入成本
- source 标记为 `user_stated` 区分于系统自动推断的记忆

### Decision 3: 经典按钮样式

```css
.classic-button {
  background: var(--classic-bg);
  border: 2px solid;
  border-color:
    var(--classic-border-light)
    var(--classic-border-dark)
    var(--classic-border-dark)
    var(--classic-border-light);
  padding: 4px 12px;
  font-size: 12px;
  font-family: inherit;
  cursor: pointer;
  border-radius: 3px;
}

.classic-button:hover {
  background: var(--classic-bg-light);
}

.classic-button:active {
  border-color:
    var(--classic-border-dark)
    var(--classic-border-light)
    var(--classic-border-light)
    var(--classic-border-dark);
  background: var(--classic-bg-dark);
}

.classic-button-primary {
  background: var(--classic-selection);
  color: var(--classic-selection-text);
}
```

### Decision 4: 经典列表/表格样式

```css
.classic-list {
  background: var(--classic-window-content);
  border: 2px solid;
  border-color:
    var(--classic-border-dark)
    var(--classic-border-light)
    var(--classic-border-light)
    var(--classic-border-dark);
}

.classic-list-row {
  padding: 4px 8px;
  border-bottom: 1px solid var(--classic-bg);
}

.classic-list-row:nth-child(odd) {
  background: var(--classic-window-content);
}

.classic-list-row:nth-child(even) {
  background: var(--classic-bg-light);
}

.classic-list-row:hover {
  background: var(--classic-selection);
  color: var(--classic-selection-text);
}
```

### Decision 5: 经典窗口标题栏

```css
.classic-window {
  border: 2px solid;
  border-color:
    var(--classic-border-light)
    var(--classic-border-dark)
    var(--classic-border-dark)
    var(--classic-border-light);
  background: var(--classic-bg);
}

.classic-window-title {
  background: linear-gradient(
    to bottom,
    var(--classic-bg-light) 0%,
    var(--classic-bg) 50%,
    var(--classic-bg-dark) 100%
  );
  padding: 4px 8px;
  border-bottom: 1px solid var(--classic-border-dark);
  font-weight: bold;
  font-size: 12px;
  text-align: center;
}

.classic-window-content {
  background: var(--classic-window-content);
  padding: 12px;
}
```

## Risks / Trade-offs

| Risk | Impact | Mitigation |
|------|--------|------------|
| 经典风格可能不被所有用户喜欢 | 部分用户可能觉得过时 | 可以在未来添加主题切换功能 |
| 信息密度增加 | 可能在小屏幕上拥挤 | 合理使用滚动和折叠 |
| 与 Ant Design 组件风格冲突 | 视觉不一致 | 使用 CSS 覆盖或自定义组件 |

## Migration Plan

1. **Phase 1**: 更新 CSS 变量和基础样式
2. **Phase 2**: 添加工作记忆面板组件
3. **Phase 3**: 重构 Tab 结构
4. **Phase 4**: 调整长期记忆面板样式
5. **Phase 5**: 测试和微调

## Open Questions

1. **是否需要主题切换**：是否提供现代/经典风格切换？建议：本次不实现，但预留扩展点
2. **Ant Design 组件处理**：是否完全放弃 Ant Design？建议：保留功能组件，用 CSS 覆盖样式
